import { Router } from 'express';

const router = Router();

// Report generation routes (to be implemented)
router.get('/', (req, res) => {
  res.json({ message: 'Report routes - to be implemented' });
});

export default router;
